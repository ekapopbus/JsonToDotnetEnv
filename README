# Json To Dotnet Environment

JsonToDotnetEnv is a extension for parse json to Environment variables in dotnet core format

# Features

  - Parse json to Environment variables

You can also:
  - new text editor and paste json format
  - (`Cmd+\` on macOS or `Ctrl+\` on Windows and Linux)+p put the command `JsonToDotnetEnv` then enter it will be replace json format with environment variables
  - done
 
## Release Notes
Users appreciate release notes as you update your extension.
### 1.0.4
Initial release of JsonToDotnetEnv

